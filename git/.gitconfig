[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential

[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential

[core]
    editor = nvim
	pager = delta --diff-so-fancy
	# pager = diff-so-fancy | less --tabs=4 -RFX
    excludesfile = /Users/elkxj/.gitignore-global
    attributesfile = ~/.gitattributes

[color]
	ui = true
    diff = auto
    status = auto
    branch = auto
    showbranch = auto

[color "branch"]
	local = green bold
	remote = cyan bold
	current = yellow bold

[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
    newHighlight = green bold 22

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse

[color "status"]
	added = green bold
	untracked = red bold
	changed = yellow bold

[delta]
    side-by-side = true
    line-numbers = true
    side-by-side = true
    syntax-theme = "Monokai Extended"
    features = line-numbers decorations
    navigate = true  # use n and N to move between diff sections
[delta "decorations"]
	minus-style = red bold normal
	plus-style = green bold normal
	minus-emph-style = white bold red
	minus-non-emph-style = red bold normal
	plus-emph-style = white bold green
	plus-non-emph-style = green bold normal
	file-style = yellow bold none
	file-decoration-style = yellow box
	hunk-header-style = magenta bold
	hunk-header-decoration-style = magenta box
	minus-empty-line-marker-style = normal normal
	plus-empty-line-marker-style = normal normal
	line-numbers-right-format = "{np:^4}│ "

[diff]
    tool = difftastic
    colorMoved = default

[difftool]
        prompt = false

[difftool "difftastic"]
        cmd = difft "$LOCAL" "$REMOTE"

[interactive]
    diffFilter = delta --color-only --diff-so-fancy
	# diffFilter = diff-so-fancy --patch

[merge]
    conflictstyle = diff3

[user]
    email = lecoqjacob@gmail.com
	name = Yendor
	signingkey = 9ACFF95F46551790

[hub]
    protocol = ssh

[commit]
	gpgsign = true
    template = ~/.git-commit-template

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[push]
    autoSetupRemote = true

# [format]
#     pretty = %C(yellow)%h%Creset %C(magenta)%cd%Creset %an <%ae> %C(cyan)%s%Creset%C(yellow)%d%Creset

[init]
	defaultBranch = main

[pager]
    difftool = true

# https://github.com/GitAlias/gitalias
[include]
	path = ~/.gitalias.txt

# Alternate identities
[include]
	path = ~/.gitidentities/identities

[url "ssh://git@github.com/"]
  insteadOf = https://github.com/

[guitool "Stash/show"]
  cmd = git stash show -p
[guitool "Stash/list"]
  cmd = git stash list
[guitool "Stash/pop"]
  cmd = git stash pop
[guitool "Stash/drop"]
  cmd = git stash drop
  confirm = yes
[guitool "Stash"]
  cmd = git stash
	
[alias]
    dft = difftool
    nuke = !git-nuke
    cr = "!f() { git fetch -p && for branch in $(git branch -vv | grep ': gone]' | awk '{print $1}'); do git branch -D $branch; done }; f"
    pnv = push --no-verify
    pnvf = push --no-verify --force
    #############
	l = log --oneline
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%ad%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(yellow)%s%C(reset) %C(dim white)- %an<%ae>%C(reset)' --all
    #############
	ours = "!f() { git checkout --ours $@ && git add $@; }; f"
	theirs = "!f() { git checkout --theirs $@ && git add $@; }; f"